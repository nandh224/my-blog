# my-blog

마크다운 파일을 읽어 정적 블로그 웹사이트로 변환하는 프로젝트.

## 목표

- `posts/` 등에 작성한 마크다운 글을 블로그 형태의 HTML 페이지로 렌더링
- 깔끔하고 읽기 좋은 디자인 (타이포그래피 중심, 여백 넉넉히)
- 다크 모드 지원 (시스템 설정 감지 + 수동 토글)
- 모바일 반응형 (작은 화면에서도 읽기 편하게)

## 기술 스택 제약

- **프레임워크 없음.** React, Vue, Next.js 등 사용하지 않음.
- 순수 HTML, CSS, JavaScript로만 구현.
- 마크다운 파싱을 위한 최소한의 라이브러리(예: marked.js 같은 단일 목적 라이브러리) 도입은
  가능하나, 꼭 필요한 경우에만 사용하고 도입 전 사용자와 상의할 것.
- 빌드가 필요한 경우(마크다운 → HTML 정적 생성)에도 별도 프레임워크 없이 간단한
  Node.js 스크립트 정도로 처리한다.

## 디자인 원칙

- 본문 가독성 최우선: 적절한 줄 길이(max-width), 줄간격, 폰트 크기
- 색상은 라이트/다크 모드 각각에 맞는 팔레트를 CSS 변수로 관리 (`prefers-color-scheme` 기반 기본값 + 수동 토글 오버라이드)
- 불필요한 장식 요소, 애니메이션 지양 — 콘텐츠가 중심
- 모바일 우선 반응형 레이아웃 (viewport meta, 유동적 폰트/여백)

## 코드 컨벤션

- 별도 지침이 없는 한 주석은 최소화하고, 자명하지 않은 이유(WHY)가 있을 때만 작성
- 새 파일 생성보다 기존 파일 수정을 우선
- 기능에 비해 과한 추상화나 설정 옵션을 만들지 않음

## 진행 상황

- [x] 프로젝트 구조 설계 (`posts/`에 마크다운, `src/build.js`가 `dist/`로 정적 생성)
- [x] 마크다운 → HTML 변환 파이프라인 (`marked` 사용, `npm run build`)
- [x] 기본 레이아웃 / 타이포그래피 / 다크모드 CSS
- [x] 반응형 스타일
- [x] 글 목록 페이지, 개별 글 페이지
